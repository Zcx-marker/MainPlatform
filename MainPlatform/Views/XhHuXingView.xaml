<UserControl x:Class="MainPlatform.Views.XhHuXingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="580" d:DesignWidth="900"
             Background="#0B1220">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MainPlatform;component/Styles/ParameterPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="14,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 弧形参数主体 -->
        <Border Grid.Row="0" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="弧形参数"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左列 -->
                    <StackPanel Grid.Column="0" Margin="0,4,16,4">
                        <!-- 线弧类型 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="线弧类型:" Width="96"/>
                            <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                                <ComboBoxItem Content="标准"/>
                                <ComboBoxItem Content="Q弧"/>
                                <ComboBoxItem Content="低弧"/>
                                <ComboBoxItem Content="自定义"/>
                            </ComboBox>
                        </StackPanel>
                        <!-- 反转高度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="反转高度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="180"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~10000)"/>
                        </StackPanel>
                        <!-- 反转角度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="反转角度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="-45"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(-45~89)"/>
                        </StackPanel>
                        <!-- 线弧高度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="线弧高度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="150"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~5000)"/>
                        </StackPanel>
                        <!-- 拉弧轨迹 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="拉弧轨迹:" Width="96"/>
                            <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                                <ComboBoxItem Content="圆弧"/>
                                <ComboBoxItem Content="直线"/>
                                <ComboBoxItem Content="抛物线"/>
                            </ComboBox>
                        </StackPanel>
                        <!-- 回转距离 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="回转距离:" Width="96"/>
                            <Button Style="{StaticResource SignButton}" Content="+"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="100" Margin="-1,0,0,0"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(±500)"/>
                        </StackPanel>
                        <!-- 弧顶线夹关闭 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <CheckBox Style="{StaticResource DarkCheckBox}" Content="弧顶线夹关闭" IsChecked="True" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(手动模式)" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </StackPanel>
                        <!-- 动作阈值 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="动作阈值:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="100"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~250)"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- 右列 -->
                    <StackPanel Grid.Column="1" Margin="16,4,0,4">
                        <!-- AF 角度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="AF 角度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="-15"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(-15~30)"/>
                        </StackPanel>
                        <!-- 反转距离 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="反转距离:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="200"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~10000)"/>
                        </StackPanel>
                        <!-- 弧高基准 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="弧高基准:" Width="96"/>
                            <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                                <ComboBoxItem Content="固定弧高"/>
                                <ComboBoxItem Content="相对焊点"/>
                                <ComboBoxItem Content="自动"/>
                            </ComboBox>
                        </StackPanel>
                        <!-- 弧高补偿 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="弧高补偿:" Width="96"/>
                            <Button Style="{StaticResource SignButton}" Content="+"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="0" Margin="-1,0,0,0"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(±1000)"/>
                        </StackPanel>
                        <!-- 弧形角度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="弧形角度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="36"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~89)"/>
                        </StackPanel>
                        <!-- 预设焊点位置 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <CheckBox Style="{StaticResource DarkCheckBox}" Content="预设焊点位置" IsChecked="True" VerticalAlignment="Center"/>
                            <TextBlock Style="{StaticResource ParamLabel}" Text="对点" Width="60" Margin="8,0,0,0"/>
                        </StackPanel>
                        <!-- 高低落差 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="高低落差:" Width="96"/>
                            <Button Style="{StaticResource SignButton}" Content="+"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="0" Margin="-1,0,0,0"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(±5000)"/>
                        </StackPanel>
                        <!-- 焊点间距 -->
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="焊点间距:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="150"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(100~5000)"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 操作栏 -->
        <Border Grid.Row="1" Background="#101B2B" CornerRadius="8" Padding="12,8" BorderBrush="#1E293B" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource GhostButton}" Content="恢复默认" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource ActionButton}" Content="保存参数"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
