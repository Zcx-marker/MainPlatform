<UserControl x:Class="MainPlatform.Views.XhQhHandianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="580" d:DesignWidth="900"
             Background="#F2F5F9">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MainPlatform;component/Styles/ParameterPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 排线设置 -->
        <Border Grid.Row="0" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="排线设置"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- 焊点数 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="焊点数:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="2"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(1-27)"/>
                    </StackPanel>
                    <!-- 排线线数 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="排线线数:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="5"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(1-20)"/>
                    </StackPanel>
                    <!-- 排线间距 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="排线间距:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="200"/>
                        <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(30-1000)"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 焊点参数 -->
        <Border Grid.Row="1" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Style="{StaticResource SectionTitle}" Grid.Column="0">
                        <Border Style="{StaticResource SectionTitleBar}"/>
                        <TextBlock Style="{StaticResource SectionTitleText}" Text="焊点参数"/>
                    </StackPanel>
                    <!-- 焊点导航 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Button Style="{StaticResource NavArrowButton}" Content="←"/>
                        <Border Background="#FFFFFF" BorderBrush="#C0C8D0" BorderThickness="1" CornerRadius="6"
                                Height="36" Width="72" Margin="8,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="第 " Foreground="#6B7A8C" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Text="1" Foreground="#1E5AA8" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                <TextBlock Text=" 焊点" Foreground="#6B7A8C" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <Button Style="{StaticResource NavArrowButton}" Content="→"/>
                    </StackPanel>
                    <!-- 两段焊接 -->
                    <CheckBox Grid.Column="2" Style="{StaticResource DarkCheckBox}" Content="两段焊接" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- 左列 -->
                    <StackPanel Grid.Column="0">
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="焊接压力:" Width="84"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="18"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="g"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(5-150)"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="焊接超声:" Width="84"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="120"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="‰"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0-1000)"/>
                        </StackPanel>
                    </StackPanel>
                    <!-- 右列 -->
                    <StackPanel Grid.Column="1">
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="焊接时间:" Width="84"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="100"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="ms"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0-1000)"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="渐进时间:" Width="84"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="5"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="ms"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0-100)"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 变形控制 -->
        <Border Grid.Row="2" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="变形控制"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Grid.Column="0" Style="{StaticResource DarkCheckBox}" Content="变形超限报警" Margin="10,4"/>
                    <CheckBox Grid.Column="1" Style="{StaticResource DarkCheckBox}" Content="变形控制使能" Margin="10,4"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 操作栏 -->
        <Border Grid.Row="3" Background="#FFFFFF" CornerRadius="8" Padding="12,8" BorderBrush="#E5E8EB" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource GhostButton}" Content="恢复默认" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource ActionButton}" Content="保存参数"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
