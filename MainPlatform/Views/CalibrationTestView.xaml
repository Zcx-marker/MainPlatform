<UserControl x:Class="MainPlatform.Views.CalibrationTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="580" d:DesignWidth="900"
             Background="#0B1220">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MainPlatform;component/Styles/ParameterPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="14,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 页面信息条 -->
        <Border Grid.Row="0" Style="{StaticResource PageInfoBar}">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="设备设定" Foreground="#475569" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock Text="  /  " Foreground="#334155" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock Text="校准测试" Foreground="#38BDF8" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Style="{StaticResource StatusBadge}" Background="#0E2A1A" Margin="0,0,12,0">
                        <TextBlock Text="设备就绪" Foreground="#22C55E" FontSize="11"/>
                    </Border>
                    <TextBlock Text="程序: wsd" Foreground="#64748B" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 运动参数 -->
        <Border Grid.Row="1" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="运动参数"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="焊接速度:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="200"/>
                        <TextBlock Style="{StaticResource ParamUnit}" Text="%"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(5~500)"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="断丝速度:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="10"/>
                        <TextBlock Style="{StaticResource ParamUnit}" Text="%"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(5~1000)"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="留尾模式:" Width="76"/>
                        <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                            <ComboBoxItem Content="线夹"/>
                            <ComboBoxItem Content="气流"/>
                            <ComboBoxItem Content="关闭"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="打火速度:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="300"/>
                        <TextBlock Style="{StaticResource ParamUnit}" Text="%"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(0~1000)"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                        <TextBlock Style="{StaticResource ParamLabel}" Text="加热功率:" Width="76"/>
                        <TextBox Style="{StaticResource ParamInput}" Text="0"/>
                        <TextBlock Style="{StaticResource ParamUnit}" Text="%"/>
                        <TextBlock Style="{StaticResource ParamRange}" Text="(0~100)"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 校准操作 -->
        <Border Grid.Row="2" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="校准操作"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- 第一行 -->
                    <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource CalibButton}" Content="水平锁紧" Margin="4"/>
                    <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource CalibButton}" Content="垂直锁紧" Margin="4"/>
                    <Button Grid.Row="0" Grid.Column="2" Style="{StaticResource CalibButton}" Content="力校准" Margin="4"/>
                    <Button Grid.Row="0" Grid.Column="3" Style="{StaticResource CalibButton}" Content="力测试" Margin="4"/>
                    <!-- 第二行 -->
                    <Button Grid.Row="1" Grid.Column="0" Style="{StaticResource CalibButton}" Content="超声校准" Margin="4"/>
                    <Button Grid.Row="1" Grid.Column="1" Style="{StaticResource CalibButton}" Content="初始化" Margin="4"/>
                    <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource CalibButton}" Content="序列号" Margin="4"/>
                </Grid>
                <!-- 校准状态（扩展） -->
                <Border Background="#0D1726" CornerRadius="8" Padding="12,10" Margin="0,12,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#22C55E" VerticalAlignment="Center"/>
                            <TextBlock Text="力校准: 已完成" Foreground="#94A3B8" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#22C55E" VerticalAlignment="Center"/>
                            <TextBlock Text="超声校准: 已完成" Foreground="#94A3B8" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#F59E0B" VerticalAlignment="Center"/>
                            <TextBlock Text="序列号: 未配置" Foreground="#94A3B8" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- 操作栏 -->
        <Border Grid.Row="3" Background="#101B2B" CornerRadius="8" Padding="12,8" BorderBrush="#1E293B" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource GhostButton}" Content="恢复默认" Margin="0,0,8,0"/>
                <Button Style="{StaticResource ActionButton}" Content="保存参数"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
