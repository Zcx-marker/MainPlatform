<UserControl x:Class="MainPlatform.Views.XhWeiSiQiuXingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="580" d:DesignWidth="900"
             Background="#F2F5F9">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MainPlatform;component/Styles/ParameterPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 尾丝/球形参数主体 -->
        <Border Grid.Row="0" Style="{StaticResource SectionCard}" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Style="{StaticResource SectionTitle}">
                    <Border Style="{StaticResource SectionTitleBar}"/>
                    <TextBlock Style="{StaticResource SectionTitleText}" Text="尾丝/球形参数"/>
                </StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左列 -->
                    <StackPanel Grid.Column="0" Margin="0,4,16,4">
                        <!-- 线尾长度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="线尾长度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="250"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~1000)"/>
                        </StackPanel>
                        <!-- 断丝距离 -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="断丝距离:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="200"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~500)"/>
                        </StackPanel>
                        <!-- 断丝超声 -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="断丝超声:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="40"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="%"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~500)"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- 右列 -->
                    <StackPanel Grid.Column="1" Margin="16,4,0,4">
                        <!-- 线尾角度 -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="线尾角度:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="30"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~60)"/>
                        </StackPanel>
                        <!-- 断丝角度（带+号） -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="断丝角度:" Width="96"/>
                            <Button Style="{StaticResource SignButton}" Content="+"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="30" Margin="-1,0,0,0"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(±45)"/>
                        </StackPanel>
                        <!-- 安全位置 -->
                        <StackPanel Orientation="Horizontal" Margin="0,6,0,6">
                            <TextBlock Style="{StaticResource ParamLabel}" Text="安全位置:" Width="96"/>
                            <TextBox Style="{StaticResource ParamInput}" Text="0"/>
                            <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                            <TextBlock Style="{StaticResource ParamRange}" Text="(0~1000)"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 操作栏 -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="8" Padding="12,8" BorderBrush="#E5E8EB" BorderThickness="1">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource GhostButton}" Content="恢复默认" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource ActionButton}" Content="保存参数"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
