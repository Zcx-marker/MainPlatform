<UserControl x:Class="MainPlatform.Views.QhHuXingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="580" d:DesignWidth="900"
             Background="#F2F5F9">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MainPlatform;component/Styles/ParameterPageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 主体：参数 + 示意图 -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧参数区 -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,8,0">
                <StackPanel>
                    <!-- 线弧设置 -->
                    <Border Style="{StaticResource SectionCard}" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Style="{StaticResource SectionTitle}">
                                <Border Style="{StaticResource SectionTitleBar}"/>
                                <TextBlock Style="{StaticResource SectionTitleText}" Text="线弧设置"/>
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="线弧类型:" Width="84"/>
                                    <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                                        <ComboBoxItem Content="Q弧"/>
                                        <ComboBoxItem Content="标准弧"/>
                                        <ComboBoxItem Content="低弧"/>
                                        <ComboBoxItem Content="自定义"/>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="反转高度:" Width="84"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="150"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(0~6000)"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 反转与补偿 -->
                    <Border Style="{StaticResource SectionCard}" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Style="{StaticResource SectionTitle}">
                                <Border Style="{StaticResource SectionTitleBar}"/>
                                <TextBlock Style="{StaticResource SectionTitleText}" Text="反转与补偿"/>
                            </StackPanel>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- 反转距离 -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="反转距离:" Width="84"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="150"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(0~3000)"/>
                                </StackPanel>
                                <!-- 反转角度（带+号） -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="反转角度:" Width="84"/>
                                    <Button Style="{StaticResource SignButton}" Content="+"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="0" Margin="-1,0,0,0"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(-30~150)"/>
                                </StackPanel>
                                <!-- 线长补偿（带-号） -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="线长补偿:" Width="84"/>
                                    <Button Style="{StaticResource SignButton}" Content="-"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="0" Margin="-1,0,0,0"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(±500)"/>
                                </StackPanel>
                                <!-- 拉弧轨迹 -->
                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="拉弧轨迹:" Width="84"/>
                                    <ComboBox Style="{StaticResource DarkComboBox}" SelectedIndex="0">
                                        <ComboBoxItem Content="圆弧"/>
                                        <ComboBoxItem Content="直线"/>
                                        <ComboBoxItem Content="抛物线"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 角度与回转 -->
                    <Border Style="{StaticResource SectionCard}" Margin="0,0,0,10">
                        <StackPanel>
                            <StackPanel Style="{StaticResource SectionTitle}">
                                <Border Style="{StaticResource SectionTitleBar}"/>
                                <TextBlock Style="{StaticResource SectionTitleText}" Text="角度与回转"/>
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="弧形角度:" Width="84"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="30"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="°"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(0~89)"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4">
                                    <TextBlock Style="{StaticResource ParamLabel}" Text="回转距离:" Width="84"/>
                                    <Button Style="{StaticResource SignButton}" Content="+"/>
                                    <TextBox Style="{StaticResource ParamInput}" Text="0" Margin="-1,0,0,0"/>
                                    <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                                    <TextBlock Style="{StaticResource ParamRange}" Text="(±1000)"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- 功能开关 -->
                    <Border Style="{StaticResource SectionCard}">
                        <StackPanel>
                            <StackPanel Style="{StaticResource SectionTitle}">
                                <Border Style="{StaticResource SectionTitleBar}"/>
                                <TextBlock Style="{StaticResource SectionTitleText}" Text="功能开关"/>
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource DarkCheckBox}" Content="弧顶线夹关闭" IsChecked="True" Margin="10,4"/>
                                <CheckBox Grid.Column="1" Style="{StaticResource DarkCheckBox}" Content="预设焊点位置" Margin="10,4"/>
                            </Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,8,0,0">
                                <TextBlock Style="{StaticResource ParamLabel}" Text="动作阈值:" Width="84"/>
                                <TextBox Style="{StaticResource ParamInput}" Text="10"/>
                                <TextBlock Style="{StaticResource ParamUnit}" Text="um"/>
                                <TextBlock Style="{StaticResource ParamRange}" Text="(0~250)"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- 右侧：线弧示意图（扩展） -->
            <Border Grid.Column="1" Style="{StaticResource SectionCard}" Margin="8,0,0,0">
                <StackPanel>
                    <StackPanel Style="{StaticResource SectionTitle}">
                        <Border Style="{StaticResource SectionTitleBar}"/>
                        <TextBlock Style="{StaticResource SectionTitleText}" Text="线弧示意"/>
                    </StackPanel>
                    <Border Background="#FFFFFF" CornerRadius="8" Padding="8">
                        <Canvas Width="240" Height="200">
                            <!-- 焊盘（第一焊点） -->
                            <Rectangle Canvas.Left="20" Canvas.Top="160" Width="50" Height="12" Fill="#8A94A3" RadiusX="2"/>
                            <TextBlock Canvas.Left="22" Canvas.Top="175" Text="1st" Foreground="#6B7A8C" FontSize="9"/>
                            <!-- 焊盘（第二焊点） -->
                            <Rectangle Canvas.Left="170" Canvas.Top="160" Width="50" Height="12" Fill="#8A94A3" RadiusX="2"/>
                            <TextBlock Canvas.Left="175" Canvas.Top="175" Text="2nd" Foreground="#6B7A8C" FontSize="9"/>
                            <!-- 线弧（Q弧示意） -->
                            <Path Data="M 45 160 C 45 80 100 40 140 50 C 170 58 195 100 195 160"
                                  Stroke="#1E5AA8" StrokeThickness="2" Fill="Transparent"/>
                            <!-- 弧顶点 -->
                            <Ellipse Canvas.Left="136" Canvas.Top="46" Width="8" Height="8" Fill="#F0A500"/>
                            <TextBlock Canvas.Left="148" Canvas.Top="42" Text="弧顶" Foreground="#F0A500" FontSize="9"/>
                            <!-- 反转点 -->
                            <Ellipse Canvas.Left="42" Canvas.Top="78" Width="6" Height="6" Fill="#2FA84F"/>
                            <TextBlock Canvas.Left="8" Canvas.Top="70" Text="反转点" Foreground="#2FA84F" FontSize="9"/>
                            <!-- 标注线 -->
                            <Line X1="45" Y1="160" X2="45" Y2="80" Stroke="#8A94A3" StrokeThickness="1" StrokeDashArray="2,2"/>
                            <TextBlock Canvas.Left="50" Canvas.Top="115" Text="反转高度" Foreground="#6B7A8C" FontSize="9"/>
                            <Line X1="45" Y1="160" X2="195" Y2="160" Stroke="#C0C8D0" StrokeThickness="1"/>
                        </Canvas>
                    </Border>
                    <TextBlock Text="当前线弧: Q弧" Foreground="#1E5AA8" FontSize="12" FontWeight="SemiBold" Margin="0,10,0,2"/>
                    <TextBlock Text="拉弧轨迹: 圆弧" Foreground="#4B5563" FontSize="11" Margin="0,2,0,2"/>
                    <TextBlock Text="弧形角度: 30°" Foreground="#4B5563" FontSize="11" Margin="0,2,0,2"/>
                    <TextBlock Text="反转高度: 150 um" Foreground="#4B5563" FontSize="11" Margin="0,2,0,2"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 操作栏 -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="8" Padding="12,8" BorderBrush="#E5E8EB" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource GhostButton}" Content="恢复默认" Margin="0,0,8,0"/>
                <Button Style="{StaticResource ActionButton}" Content="保存参数"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
